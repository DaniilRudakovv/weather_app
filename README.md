Weather App 🌤️
Приложение для получения текущей погоды, истории запросов и экспорта данных в CSV.
С поддержкой кэширования, rate limiting и удобной фильтрации.

📦 Запуск через Docker Compose

Собрать и запустить все сервисы

docker-compose up --build

Приложение будет доступно на: http://127.0.0.1:8000

Для остановки и удаления контейнеров:

docker-compose down

🔗 Доступные Endpoints

1️⃣ Получение погоды

Запрос текущей погоды для города

Пример: Celsius

curl -X POST "http://127.0.0.1:8000/api/v1/weather" \
     -H "Content-Type: application/json" \
     -d '{"city_name": "Moscow", "unit": "celsius"}'
     
Пример: Fahrenheit

curl -X POST "http://127.0.0.1:8000/api/v1/weather" \
     -H "Content-Type: application/json" \
     -d '{"city_name": "New York", "unit": "fahrenheit"}'
     
2️⃣ История запросов

Получение истории запросов с фильтрацией по городу и дате.

Параметры:

city — название города (опционально)

date_from — дата начала периода (опционально, формат ISO)

date_to — дата конца периода (опционально, формат ISO)

page — номер страницы (по умолчанию 1)

per_page — элементов на странице (по умолчанию 10, max 100)

Пример: все запросы по Москве за последние 7 дней

curl -X GET "http://127.0.0.1:8000/api/v1/history?city=Moscow&date_from=2025-11-13&date_to=2025-11-20"

3️⃣ Экспорт истории

Экспорт истории запросов в CSV.

Пример:

curl -X GET "http://127.0.0.1:8000/api/v1/export?city=Moscow&date_from=2025-11-13&date_to=2025-11-20" \
     -o weather_history.csv
     

4️⃣ Health Check

Проверка работоспособности сервиса.

curl -X GET "http://127.0.0.1:8000/api/v1/health"

Для запуска тестов:
pytest -q

